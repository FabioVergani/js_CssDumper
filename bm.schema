((w)=>{
 const p='cssdump';
 (w[p]||(w[p]=(()=>{
	function inibit(e){e.stopImmediatePropagation();e.stopPropagation();e.preventDefault();return false}
	const c=w.console||((f)=>{return{log:f,info:f,group:f,groupEnd:f}})(()=>{});
	const events=[];
	let dumps=0,dumping=false;
	return (w)=>{
	 if(dumping!==true){
		dumping=true;
		c.group(p,++dumps);
		//..
		c.groupEnd();
		dumping=false;
	 };
	};
 })()))(w);
})(window);
//
document.getElementById('button').onclick=window.cssdump;
